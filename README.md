# Exam Preparation Dashboard

Small Next.js + Tailwind frontend for one student's half-yearly preparation.

## Run
npm install
npm run dev

Open http://localhost:3000

Progress is currently stored in browser localStorage. Later, replace the small progress layer with Supabase so both parents and the student see the same data.
